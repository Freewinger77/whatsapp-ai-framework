{
  "name": "proxy-tracker",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "npx --yes serve -l 4173 .",
    "build": "mkdir -p public && cp index.html proxies.json public/",
    "vercel-build": "npm run build",
    "start": "npx --yes serve -l 4173 ."
  }
}
