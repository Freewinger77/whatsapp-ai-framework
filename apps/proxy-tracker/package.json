{
  "name": "proxy-tracker",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "npx --yes serve -l 4173 .",
    "build": "mkdir -p dist && cp index.html proxies.json vercel.json dist/ && cp -r public dist/public 2>/dev/null || true",
    "start": "npx --yes serve -l 4173 ."
  }
}
